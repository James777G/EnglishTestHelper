/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import StartPage from "./react/pages/App";

AppRegistry.registerComponent(appName, () => StartPage);
